name: mtc_analytics
description: Package to manage analytics tools
version: 1.0.0+2
homepage: https://github.com/crab-team/mtc-analytics

environment:
  sdk: ">=2.18.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  amplitude_flutter: ^3.10.0
  firebase_analytics: ^9.3.7
  firebase_core: ^1.24.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: null
